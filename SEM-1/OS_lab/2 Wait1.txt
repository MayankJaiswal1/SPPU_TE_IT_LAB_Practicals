#include <stdio.h>
#include <sys/wait.h>
#include <unistd.h>

int main()
{
    if (fork() == 0) // Corrected comparison operator

    {
        printf("HC: hello from child\n");
    }
    else
    {
        printf("HP: hello from parent\n");

        wait(NULL); // Corrected semicolon

        printf("CT: child has terminated\n");
    }
    printf("Bye\n");

    return 0;
}
